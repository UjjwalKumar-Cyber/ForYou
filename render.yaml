services:
  - type: web
    name: foryou
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: foryou-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: MESSAGE_PAGE_PASSWORD
        sync: false
      - key: ADMIN_PASSWORD
        sync: false

databases:
  - name: foryou-db
    plan: free
    databaseName: foryou
    user: foryou
    ipAllowList: []
